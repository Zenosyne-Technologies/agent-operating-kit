# Marvin's project memory

Marvin's own notebook — written and tidied by Marvin only. One entry per noteworthy fact: `- YYYY-MM-DD — <fact> (<ref: commit/issue/file>)`. Consult at session start; write during work and before context compaction; prune stale entries and merge duplicates when tidying. Never duplicate what the repo, tracker, or handbooks already record.

## Entries

(none yet)
