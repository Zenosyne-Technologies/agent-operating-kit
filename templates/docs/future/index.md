---
doc: Future index
type: reference
status: active
summary: Ideas and scope deliberately deferred — not scheduled, not forgotten, and explicitly not commitments.
keywords: [index, future, deferred, backlog, ideas]
level: planning
created: {{INSTALL_DATE}}
updated: {{INSTALL_DATE}}
---

# `.docs/future/`

**Belongs here**: an idea, capability or direction that was considered and deliberately put off — with the reason it was deferred and what would have to change for it to come back. Nothing here is a commitment; treat every document in this folder as explicitly NOT scope.

**Does NOT belong here**: anything the project has committed to, which belongs on the tracker and in `../plans/`; a deferred cleanup with identified debt behind it (→ `../refactor/`); and the tracker's own backlog — an unscheduled issue is a tracker item, not a document. When a deferred idea is picked up, it moves to a plan and this document flips to `superseded_by:`.

| item | what it covers | status | updated |
|---|---|---|---|
