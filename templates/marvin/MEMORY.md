# Marvin's project memory

Marvin's own notebook — written and tidied by Marvin only. One entry per noteworthy fact: `- YYYY-MM-DD — <fact> (<ref: commit/issue/file>)`. Consult at session start; write during work, before context compaction, and before a session ends (the handoff CLAUDE.md's memory rule defines); prune stale entries and merge duplicates when tidying. Never duplicate what the repo, tracker, or handbooks already record.

## Entries

(none yet)
