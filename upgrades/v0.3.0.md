# v0.3.0 — .docs move + git discipline

From 0.2.0:
1. `git mv docs/agents .docs/agents`; rewrite every `docs/agents/` reference
   in CLAUDE.md.
2. CLAUDE.md standing rules gain milestone-branching (`milestone/<slug>`)
   and autocommit.
3. briefing.md gains the branch+autocommit ingredient.
4. Severity→native mapping guidance added tracker-side.
