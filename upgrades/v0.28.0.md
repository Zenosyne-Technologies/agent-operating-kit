# v0.28.0 — visual validation (authoring spine) + a `finding:` label axis

Consumer-facing; nothing you already have breaks. Adds an **advisory visual/design validation stage** for UI-touching tasks and a dedicated `finding:` reporting axis, landing through the normal upgrade refresh:

- `claude plugin update marvin` — adds the `marvin:validator-visual` persona (Opus-tier) and the refreshed templates.
- The upgrade step-4 refresh re-renders `.marvin/agents/` guides: `visual-validation.md` (new), `validation-agent.md`, `briefing.md` (new `SURFACES:` line in the FINAL MESSAGE spec), `guardrails.md` (validator-visual row), and `label-syntax.md` (→ v1.4.0). Your customised copies are backed up first.
- Seeds `.docs/handbooks/developer/screens/index.md` where missing — the screen catalog the review resolves surfaces against; fill in your project's screens (empty is fine until you do).
- `.marvin/PROJECT-INFO.md` gains two keys — `screens_guide` and `visual_validation` (`per-task|milestone|off`, default per-task); the `project-info` skill adds them on its next validate.
- CLAUDE.md gains the conditional visual-validation lifecycle step (UI tasks only, advisory — it never gates or bounces a task); merged in, your own rules untouched.
- New label `finding:visual` (registry v1.4.0) — an orthogonal axis for design findings; apply it to issues the visual validator raises.
