# v0.26.1 — marvin's plugin manifest now declares its licence

Patch. No consumer-visible installed-file changes and no migration: the only change is that `marvin`'s plugin manifest (`.claude-plugin/plugin.json`) now declares `"license": "LicenseRef-PolyForm-Noncommercial-1.0.0"`, matching the authoritative LICENSE file and the token-telemetry manifest. `claude plugin update marvin` picks it up; there is nothing to do.
