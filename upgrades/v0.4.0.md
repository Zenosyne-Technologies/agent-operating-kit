# v0.4.0 — PM tool selection + tracker-config

From 0.3.0:
1. Install `.docs/agents/tracker-config.md` for the project's tracker
   (levels vs 4-level target, virtual milestones for Jira, severity
   mapping).
2. Linear templates restored alongside Jira.
