# v0.21.0 — Marvin's machinery moves to `.marvin/`

Files that exist only so Marvin can operate leave `.docs/`. Idempotent — skip
any step whose destination already exists.

1. `git mv .docs/agents .marvin/agents`.
2. `git mv .docs/PROJECT-INFO.md .marvin/PROJECT-INFO.md`.
3. `git mv .docs/marvin/MEMORY.md .marvin/MEMORY.md`, then remove the emptied `.docs/marvin/`.
4. Per audience: `git mv .docs/handbooks/<audience>/INDEX.md .docs/handbooks/<audience>/index.md`
   (case-only rename — go via a temp name on case-insensitive filesystems).
5. Rewrite every stale `.docs/agents/`, `.docs/PROJECT-INFO.md`, `.docs/marvin/` and handbook
   `INDEX.md` reference in `CLAUDE.md` and in the migrated files to the new paths.
6. `.docs/project-management/`, `.docs/reports/` and the handbook pages STAY — project record,
   not machinery. No tracker, label or agent changes.
