# v0.21.0 — Marvin's machinery moves to `.marvin/`

Files that exist only so Marvin can operate leave `.docs/`.

1. `.docs/agents` → `.marvin/agents`.
2. `.docs/PROJECT-INFO.md` → `.marvin/PROJECT-INFO.md`.
3. `.docs/marvin/MEMORY.md` → `.marvin/MEMORY.md`, then remove `.docs/marvin/` if now empty.
4. Per audience: `.docs/handbooks/<audience>/INDEX.md` → `index.md` (case-only rename).
5. Rewrite every stale `.docs/agents/`, `.docs/PROJECT-INFO.md`, `.docs/marvin/` and handbook
   `INDEX.md` reference in `CLAUDE.md` and the migrated files.
6. `.docs/project-management/`, `.docs/reports/` and handbook pages STAY. No tracker/label changes.

Perform these ONLY under the five rules in the `upgrade-agent-os` skill's **Migration mechanics**
section. Skipping any one silently loses a handbook index, nests `.marvin/agents/agents/`, or aborts
the run mid-way.
