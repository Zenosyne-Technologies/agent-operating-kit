# v0.21.0 — Marvin's machinery moves to `.marvin/`

Files that exist only so Marvin can operate leave `.docs/`: the kit's cascade →
`.marvin/agents/`, `PROJECT-INFO.md` + `MEMORY.md` → `.marvin/`, each handbook's
`INDEX.md` → `index.md`, plus the references to files that actually moved. Everything else
STAYS: `.docs/project-management/`, `.docs/reports/`, handbook pages, consumer files.

Do NOT do this by hand — run the plugin's script (`--check` = dry run, any tree):

    bash ${CLAUDE_PLUGIN_ROOT}/scripts/migrate-v0.21.0.sh

Exit 0 done · 2 dirty tree (a real run refuses; `--check` still prints its plan) · 3
collisions · 4 bad flags · 6 symlink/out-of-repo path and 8 unwritable file, both refused
untouched · 7 failed and rolled back to HEAD — it never leaves a half-migration. Copy its
`declined:`/`collision:`/`symlink:` lines verbatim. Why: `upgrade-agent-os` § *Migration mechanics*.
