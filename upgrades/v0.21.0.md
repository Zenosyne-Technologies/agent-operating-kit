# v0.21.0 — Marvin's machinery moves to `.marvin/`

Files that exist only so Marvin can operate leave `.docs/`: the cascade → `.marvin/agents/`,
`PROJECT-INFO.md` + `MEMORY.md` → `.marvin/`, handbook `INDEX.md` → `index.md`. Project record
(`.docs/project-management/`, `.docs/reports/`), handbook pages and consumer files STAY.

Do NOT move files by hand — run the plugin's script (`--check` = dry run, any tree):

    bash ${CLAUDE_PLUGIN_ROOT}/scripts/migrate-v0.21.0.sh

It moves and STAGES; it edits no content and commits nothing. Then update every path it
lists under `references-to-update:` yourself, judging each hit in context (a third-party
URL containing `docs/agents/` is not a kit path), and commit those edits with the staged
renames as ONE commit. Exit 0 done · 2 dirty tree · 3 collisions · 6 symlink · 9 unsafe repo
state (6 and 9 refuse untouched) · 7 rolled back. Flow: `upgrade-agent-os` step 3.
