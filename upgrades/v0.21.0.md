# v0.21.0 — Marvin's machinery moves to `.marvin/`

Files that exist only so Marvin can operate leave `.docs/`: the kit's cascade →
`.marvin/agents/`, `PROJECT-INFO.md` + `MEMORY.md` → `.marvin/`, each
`.docs/handbooks/<audience>/INDEX.md` → `index.md`, and every stale reference in
CLAUDE.md and the migrated files rewritten. `.docs/project-management/`,
`.docs/reports/`, the handbook pages and consumer files STAY. No tracker changes.

Do NOT do this by hand — run the plugin's script (add `--check` for a dry run):

    bash ${CLAUDE_PLUGIN_ROOT}/scripts/migrate-v0.21.0.sh

Exit 0 done (or already migrated) · 2 refused, dirty tree · 3 collisions to
reconcile by hand · 5 a move failed. Copy its `declined:`/`collision:` lines into
the upgrade report verbatim. Guard rationale: `upgrade-agent-os` § *Migration mechanics*.
