# v0.5.0 — Label registry + PROJECT-INFO

From 0.4.0:
1. Install `.docs/agents/label-syntax.md` (registry v1.0.0); `origin:*`
   supersedes `found-by:*` — offer the relabel sweep (mapping:
   found-by:agent-qa→origin:agent-qa, found-by:owner→origin:architect-request).
2. Re-run the intake brief to seed new labels (type:feature, origin:*).
3. Create `.docs/PROJECT-INFO.md` (list-style meta page).
4. CLAUDE.md cascade line for the registry.
