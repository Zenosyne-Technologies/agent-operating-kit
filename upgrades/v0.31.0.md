# v0.31.0 — escalate on failure pattern; an orchestrator context diet

Consumer-facing: **dispatch defaults change again.** `size:m` splits into a heavy-tier core plus small/micro pieces, research splits into a small-tier survey plus heavy synthesis (l/xl only), and a failed round is read for its PATTERN, not just counted.

- `claude plugin update marvin` — refreshes every `.marvin/agents/` cascade file (customised copies backed up first).
- **Re-sync CLAUDE.md's NINE changed kit-authored parts** with the current template (merge, never overwrite; your own rules stay untouched): *You are Marvin* — memory paragraph gains the one-session-per-milestone handoff; *Model-tier dispatch* — orchestrator line adds "never bulk-reads" + the reader-sub-agent rule, the three tier bullets move size:m's peripheral pieces/corrections/brief-falsifier to the small tier and every tracker call/release step to ponytail, and the Escalate line becomes pattern-driven (converging/stuck/whack-a-mole/bloat/loop/cost blowout), citing new `escalation-ladder.md`; *Task lifecycle* — FAIL-return sentence adds the small-tier fix-list route and "counts as one failed round", DoD line adds the size:m decomposition + brief-falsifier requirement; *Rules cascade* — planning-research row rescoped to `size:m`+; *Standing rules* — git/release row adds run-report pruning (`briefing.md` item 11), and the chat rules fold the real-bug bullet into "Nothing important stays in chat" and add the new **Talking to the user** rule (`user-updates.md`).
- Add `.marvin/runs/` to `.gitignore` (the upgrade skill does this for you) — gitignored sub-agent run reports, pruned at milestone close.
- New files: `escalation-ladder.md` (the ladder rungs, split out of `escalation.md`, which now holds only the round signals) and `user-updates.md` (emoji legend, shipped table, issue callout, Next/Open questions).
- Refreshed guides beyond the split: `guardrails.md`, `planning-research.md`, `ponytail.md`, `briefing.md`, `document-standard.md`, `git-strategy.md`, `ticket-filing.md`, `token-economics.md`, your tracker's `tracker-config.md` (publishing-authority wording), every persona file, and the install/upgrade/report/stress-test/validate-kit/project-info skills.

This note's scope was derived over `v0.30.0..HEAD`, non-merge commits only.
