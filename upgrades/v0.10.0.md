# v0.10.0 — Security + two-stage validation

From 0.9.0:
1. Install `.docs/agents/security.md` + cascade line.
2. briefing gains the security-surface ingredient (items renumber 1–10).
3. validation-agent becomes sequenced stages (completion FIRST, security
   only after pass); CLAUDE.md lifecycle line updated.
