# v0.19.0 — personas ship with the plugin (marvin:* namespace)

1. Merge the updated dispatch and lifecycle wording into CLAUDE.md: persona
   references gain the plugin namespace — `marvin:developer`,
   `marvin:developer-small`, `marvin:validator-completion`,
   `marvin:validator-security`, `marvin:researcher`, `marvin:documenter`,
   `marvin:ponytail`.
2. Delete the seven persona files v0.18.0 copied into `.claude/agents/`
   (developer.md, developer-small.md, validator-completion.md,
   validator-security.md, researcher.md, documenter.md, ponytail.md) — ONLY
   where the content matches the kit's copies; never delete a project's own
   agent definitions. The personas now come with the plugin itself.
3. Nothing to copy or resolve. Restart applies only when the marvin PLUGIN was
   just installed/updated this session — not per project.
