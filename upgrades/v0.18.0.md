# v0.18.0 — named sub-agent personas

1. Copy `templates/agents/` → `<repo>/.claude/agents/` (merge — never overwrite a
   same-named agent the project already defines), resolving each persona's `model:`
   placeholder from the install's tier mapping (`{{ESCALATION_MODEL}}` → heavy,
   `{{WORKER_MODEL}}` → small, `{{MICRO_MODEL}}` → micro; aliases like `opus`,
   `sonnet`, `haiku`).
2. Merge the updated dispatch and lifecycle wording into CLAUDE.md: execution routes
   by NAMED persona (`developer`, `developer-small`, `validator-completion`,
   `validator-security`, `researcher`, `documenter`, `ponytail`); a generic
   sub-agent dispatch is an error.
3. No tracker or label changes. If telemetry is enabled, events start carrying the
   persona name automatically (SubagentStop hook) — no telemetry-side steps.
