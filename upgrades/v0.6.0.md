# v0.6.0 — Sizing + planning research

From 0.5.0:
1. Registry → v1.1.0 (`size:` dimension); re-run intake to seed size
   labels.
2. Install `.docs/agents/planning-research.md` + its cascade line.
3. tracker-config gains the size→native estimate table.
