# v0.20.0 — documentation is a gate, not a suggestion

1. Merge the updated **Task lifecycle** wording into CLAUDE.md: every arrow is a
   gate — security validation only after completion PASSES, **documentation
   only after BOTH validators pass, and the tracker issue closes only after
   documentation lands**. Any FAIL returns the task to its build tier with the
   findings and re-enters at validate-completion (never at document or close).
   A task is Done only when both validators passed AND its docs landed.
2. Replace `.docs/agents/documentation-agent.md`'s opening paragraph and add
   the "After a verdict" section to `.docs/agents/validation-agent.md` from the
   current templates — the two sides of the same hand-off.
3. No tracker, label or agent changes.
