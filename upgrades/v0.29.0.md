# v0.29.0 — visual capture made executable + the `visual-sweep` fan-out scenario

Consumer-facing; nothing you already have breaks. Completes the visual-validation subsystem's capture path (v0.28.0 shipped its authoring spine).

- `claude plugin update marvin` — picks up the new **`/marvin:play visual-sweep`** scenario (enumerate every UI surface → fan out `marvin:validator-visual`, hard cap 4/round → aggregate ONE whole-project design report in `.docs/reports/`) and the refreshed play contract. Run it with `/marvin:play visual-sweep`.
- The upgrade step-4 refresh re-renders `.marvin/agents/visual-validation.md`: the capture section now names the concrete browser-pane tools (`preview_start` / `navigate` / `resize_window` presets + `colorScheme` / `computer` screenshot) so a validator executes it without guessing, ties each degradation exit to an observable browser outcome, and adds a **Playwright fallback** path + selection rule for projects that already script E2E. Your customised copy is backed up first.
- No new PROJECT-INFO keys, no file moves, no label changes — nothing to do beyond the plugin update.
