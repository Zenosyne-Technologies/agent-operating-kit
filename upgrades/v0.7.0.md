# v0.7.0 — Machine contract + idempotency

From 0.6.1:
1. Rewrite `.docs/PROJECT-INFO.md` as YAML frontmatter (13 keys) + body;
   stamp `kit_version` and `label_syntax_version`.
2. Registry → v1.2.0 (sizing rubric).
3. Intake briefs are now idempotent (re-runnable as label sync).
4. Jira installs: `convert-milestones` brief available for the future
   release-capable connector.
