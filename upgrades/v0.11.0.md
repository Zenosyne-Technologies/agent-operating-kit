# v0.11.0 — Size-routed tier economy

From 0.10.0:
1. CLAUDE.md dispatch reworded: heavy worker = size:m+/research/
   validators; small worker = size:s/tests/docs; escalation after two
   failures → frontier; milestone validation = frontier + small workers
   (validation-agent gains the section).
2. planning-research size:l row → heavy tier.
3. "default worker" wording → small worker across the cascade.
