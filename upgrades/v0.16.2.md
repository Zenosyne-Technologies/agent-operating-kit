# v0.16.2 — Issue keys in milestone branch names

From v0.16.1:
1. Milestone branches are `milestone/<KEY>-<slug>` (container epic/issue key in the name); update CLAUDE.md branching rule, briefing.md ingredient 6, and token-economics.md milestone scoping. Existing `milestone/<slug>` branches keep working (prefix rollups unchanged); rename only if convenient.
